# Handoff - GS Safety Checklist - 2026-06-11

## Read First

- Repository: `https://github.com/spahdhkd-sketch/shipyard-checklist.git`
- Working branch: `main` — last commit `ce8d339` (tag `v1.2-20260611`), **GitHub = 로컬 = 운영 모두 동일**
- Production: `https://gs-safety-checklist.vercel.app` (Vercel project `index-html`, org `team_yM4EKRXjJA9N1XsVvJBkpcB8`, 배포 완료·확인됨)
- Supabase project ref: `yuuroocvxvzgmsdeeiws`
- App version: `1.2-20260611-view-split` / asset token `20260611-view-split-1`
- 비밀키·토큰·사번을 절대 노출하지 말 것. GitHub 푸시는 사용자 PC에서 `git push origin main` (자격 증명은 사용자 PC에만 있음).
- 배포 요청("배포 하자") 없이는 APP_VERSION/asset token을 올리지 말 것 (`VERSION.md` 규칙).

## How To Resume On Another Desktop

```powershell
git clone https://github.com/spahdhkd-sketch/shipyard-checklist.git   # 또는 기존 폴더에서 git pull
cd shipyard-checklist
npm.cmd install        # devDeps: esbuild, puppeteer-core
npm.cmd run verify     # 정적 검사 + 14개 테스트 스위트 전부 통과해야 함
npm.cmd run e2e        # 실제 크롬에서 E2E 스모크 (이제 헤르메틱 — 운영 DB와 완전 차단)
```

## What Changed This Session (2026-06-11)

1. **app-v2.js 모듈 분할 4단계 완료** (12,531 → 12,134줄):
   - `76effaf` 푸시: 순수 로직 14개+상수 2개 → `assets/js/push-rules.js`(신규, window.PushRules), 푸시 마크업 5종 → `screen-views.js`. `tests/push-rules.test.js` 추가.
   - `37a7ee2` 작업준비/점검기록 렌더러 7종 → `screen-views.js` (18개 시나리오 바이트 동일 검증).
   - `582c61f` 디스패치 영역(~631줄) 분리: `dispatchAction` 액션맵 → 도메인 디스패처 6개(워커세션/푸시/작업준비/호선/점검/이력관리, 본체 11줄), 486줄 document 클릭 리스너 → 순서 보존 핸들러 11개(본체 23줄). **클릭 체인은 순서·다중실행 민감 — 함부로 재배열 금지.**
   - `7a0a69b` renderItems(2분할)·renderSectionManager·WorkPrepStatusControl·WorkPrepTimeline → `screen-views.js`.
2. **E2E 헤르메틱화** (`32ebb73`, `8acd67e`): 근본 원인 — 앱 부팅 시 `pullRemote`가 REMOTE_AUTHORITATIVE_KEYS 테이블을 서버 데이터로 통째 교체 → E2E 시드가 운영 데이터에 덮임. 수정: e2e-smoke.mjs가 localhost 외 모든 요청 차단(supabase 읽기/쓰기 모두) + 차단 동작 자체를 assert. 사용자 PC에서 전체 통과 확인.
3. **카드 요약 버그 수정** (`ef34789`): "전원 점검 완료"가 레코드 status(used)만으로 표시되던 것 → 실제 제출 진행률(`submissionProgress.complete`) 기준으로만 표시.
4. **운영 배포** (`ce8d339`): 1.2-20260611 + 새 토큰/SW 버전, Vercel 배포·alias·서빙 확인 완료.
5. **운영 Supabase 점검**: E2E 테스트 데이터 오염 의심 → 전수 조회 결과 **깨끗함** (w-kim/홍길동/wp-e2e/2401/2402 모두 없음, RLS가 쓰기를 막았던 것). 정리 불필요.

## Known Issues / 환경 주의사항

- **`deploy-prod.bat`이 사용자 PC에서 실패** (원인 미파악 — 에러 메시지 확보 필요). 우회: PowerShell에서 `npm run build:assets` → `vercel --prod` → `vercel alias set <URL> gs-safety-checklist.vercel.app` 수동 실행은 정상 동작.
- **Cowork virtiofs 마운트에서 대용량 파일 편집 주의**: Write/Edit 도구가 큰 파일을 무음 절단할 수 있음(2026-06-10 사고). 셸(bash/python3)로 쓰고 `node --check`로 재검증할 것.
- 마운트에서 git이 lock 파일(.git/index.lock, ORIG_HEAD)을 못 지워 머지가 깨질 수 있음 → 잠금 파일 삭제 후 `git clean -fd` + 재머지로 복구.
- 샌드박스 e2e: 백그라운드 프로세스는 bash 호출 종료 시 동결됨. e2e는 포그라운드로 실행할 것. `@sparticuz/chromium`을 `--no-save`로 설치해 사용 가능.

## Next Work (pick up here)

1. **점검 플로우 상태 로직 분할** — `submitInspection`(~90줄), `setupSignaturePad`(~93줄), `startCheckFromWorkPrepRecord`. 상태·이벤트 결합이 깊어 순수 로직만 골라 rules 모듈로. 가장 신중 필요.
2. **`normalizePendingSyncQueue`(~108줄)** — 순수에 가까움. `sync-rules.js` 신규 모듈 후보 (단독 함수면 모듈 신설 가치 판단 먼저).
3. **데이터 정규화 묶음** — `normalizeDataShape`(63), `migrateOldChecklists`(39) 등.
4. **deploy-prod.bat 디버깅** — 사용자에게 에러 메시지 받아 수정.
5. (장기) H3394 같은 케이스 보완 — status=used인데 미제출자가 있으면 관리 화면에서 표시/재동기화하는 UX 검토.

## 분할 작업 패턴 (반드시 따를 것)

- 마크업: `screen-views.js`에 순수 `renderXxxView(model, deps)` 추가, app-v2엔 뷰모델 빌더만. data-action/클래스/id 바이트 동일 유지.
- 순수 로직: `*-rules.js` UMD 패턴(checklist-rules.js 참고), 전용 테스트 추가.
- 등록 체크리스트: 11개 HTML `<script>`(dist .min.js + ?v=토큰, app-v2보다 앞) + `sw.js` SHELL + `package.json` verify 체인 + `tests/static-recovery.test.js` 갱신 + `node tools/build-assets.mjs`로 dist 재생성(커밋 대상).
- 검증: git HEAD에서 원본 추출 → 스텁 상태로 신구 출력 바이트 비교 하니스 → `npm run verify` → 커밋.

## How To Deploy ("배포 하자" 요청 시)

`VERSION.md` 규칙: 마이너 +0.1, `<버전>-<YYYYMMDD>`, 새 asset token + SW 버전, VERSION.md 갱신, 커밋 + `v<버전>` 태그, main+태그 푸시, 사용자 PC에서 `vercel --prod` + alias, 운영 URL 검증.
