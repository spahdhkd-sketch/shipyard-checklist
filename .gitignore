.vercel
node_modules/
**/node_modules/
**/deno.lock
.worktrees/
supabase/.temp/
visual-verification/
.tmp-chrome-*/
docs/quality/claude-cowork-review-2026-05-24.md

# Local env (secrets) - never commit
.env.local
.env*.local

# Build artifacts: committed so the GitHub deploy path serves them.
# Regenerate with `npm run build:assets` before committing a release.
