# Prompt for Tomorrow's Codex

다른 데스크탑의 Codex에서 아래 프롬프트로 시작하세요.

```text
GitHub에서 `spahdhkd-sketch/shipyard-checklist` 저장소를 다운로드하고,
브랜치 `codex/safety-pictograms-handoff-20260528`를 체크아웃한 뒤,
먼저 `codex-handoff-2026-05-28.md` 문서를 열어봐.

그 문서를 현재 상태의 기준으로 삼고, 이어서 `git status --short`,
`npm.cmd run verify`, `npm.cmd run harness:live`로 로컬/운영 상태를 확인해.
작업을 시작하기 전에는 실제 production alias가 `gs-safety-checklist.vercel.app`이고
Vercel 프로젝트가 `index-html`인지 다시 확인해.
```
