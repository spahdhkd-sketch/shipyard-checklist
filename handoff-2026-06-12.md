# Handoff - GS Safety Checklist - 2026-06-12

## Read First

- Repository: `https://github.com/spahdhkd-sketch/shipyard-checklist.git`
- Branch `main` 최신 커밋: `7835248` — GitHub = 로컬(Documents\shipyard-checklist-newdesign) 동일
- **운영(gs-safety-checklist.vercel.app)은 `ce8d339`(v1.2-20260611) 기준 — 오늘 작업 3커밋은 미배포 상태**
- Supabase ref: `yuuroocvxvzgmsdeeiws` / Vercel project `index-html` (org `team_yM4EKRXjJA9N1XsVvJBkpcB8`)
- 환경 주의사항·작업 패턴·배포 절차는 `handoff-2026-06-11.md` 참조 (전부 유효)

## What Changed (2026-06-12)

1. `8ed3114` **월간 작업자 점검 현황 집계 수정**: 작업지시서 참여가 있는 날만 대상/누락으로 집계, 작업 없는 날·주말은 '제외'. 순수 분류기 `monthlyWorkerDayStatus`는 analytics-model.js, 의무 판정 `workerHasWorkPrepObligation`은 app-v2.js.
2. `fc261c8` **서약 화면 과거 날짜 조회**: ◀이전 날/날짜 선택/다음 날▶ + 오늘로 버튼. 과거 날짜는 읽기 전용(미완료자 알림 숨김). 상태 `state.pledgeViewDate`(비영속), 디스패처 `dispatchPledgeManagerAction`.
3. `7835248` **기간 이력 범위 로딩**: 통계·서약 화면이 열릴 때 해당 기간 inspections를 Supabase에서 범위 조회해 읽기 전용 캐시(`state.archivedInspections`)에 병합. 이력 화면의 20건 제한(DEFAULT_REMOTE_LIST_LIMIT)과 분리, pullRemote의 authoritative 교체와 충돌 없음. 실패 시 기존 동작으로 우아하게 강등(헤르메틱 e2e 통과).
4. 운영 DB 검증: 6/12 기준 실제 누락은 김학훈 6/10 1건뿐. 작업자별 기대값은 세션 기록 참조.

## Next Work

### 1. 작업지시서 타임라인 재설계 (사용자 스펙 확정됨, 코드 미착수)
사용자가 원하는 구조 — 상태 배지가 붙는 3개 마일스톤:
- **등록**: 작업지시서 등록 시점 + 등록자 (현재 '등록' 줄은 가상 합성이며 *현재* 상태로 문구를 만들어 시점이 왜곡되는 버그 있음 → 등록 시 실제 저장으로 전환하며 수정)
- **점검 시작**: 한 줄 + 행위자 명단 누적 방식 (첫 시작 시각 기록, 이후 시작자는 그 줄의 명단에 추가)
- **점검 완료**: 등록된 참여자 전원 제출 시점 (행위자 명단 포함)
- 관리자 수동 상태 변경 줄은 **계속 별도 기록** (자동/수동 구분 필드 추가 권장)
- 참고: 현 구조의 약점 4가지(등록 줄 시점 왜곡, 내용 수정 미기록, actor가 이름 문자열뿐, 배열 덮어쓰기 last-write-wins)를 세션에서 진단했음. entry에 `kind`/`actors`(worker id 배열) 필드 추가 + 구버전 항목 호환 정규화가 핵심.
- **단, 사용자가 "다시 설계해야 할 듯"이라 했으므로 구현 전에 설계안을 먼저 보여주고 확인받을 것.**

### 2. 배포 (요청 시)
오늘 3커밋(통계 수정 + 서약 과거 조회 + 범위 로딩)이 미배포. "배포 하자" 요청이 오면 VERSION.md 규칙대로 1.3-YYYYMMDD + 새 에셋 토큰/SW 버전 → 커밋·태그·푸시 → 사용자 PC에서 `vercel --prod` + alias (deploy-prod.bat은 원인 미상으로 실패 — 에러 메시지 받아서 수정 필요).

### 3. 이월 항목 (handoff-2026-06-11 참조)
점검 플로우 상태 로직 분할, normalizePendingSyncQueue 분리, deploy-prod.bat 디버깅.
