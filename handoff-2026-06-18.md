# Handoff - GS Safety Checklist - 2026-06-18

## Read First

- Repository: `https://github.com/spahdhkd-sketch/shipyard-checklist.git`
- Branch `main` — **운영(gs-safety-checklist.vercel.app) = GitHub origin/main = `343f8c4`(v1.3-20260618)로 일치·배포 완료·확인됨.**
- Vercel project `index-html` (org `team_yM4EKRXjJA9N1XsVvJBkpcB8`) / Supabase ref `yuuroocvxvzgmsdeeiws` (둘 다 live 확인).
- App version: `1.3-20260618-stats-pledge` / asset token `20260618-stats-pledge-1`.
- 비밀키·사번·토큰 노출 금지. GitHub 푸시·Vercel 배포는 **사용자 PC에서만** (자격 증명은 PC에만 있음).
- 배포 요청("배포 하자") 없이는 APP_VERSION/asset token을 올리지 말 것 (`VERSION.md` 규칙).

## ⚠️ 미푸시 리팩토링 2건 (운영과 무관, 동작 동일)

오늘 만든 모듈 분할 2커밋은 **로컬에만 있고 origin/main 미반영**. 패치로 전달됨.
```
1c8eddc  refactor: extract data-shape normalizers from app-v2
bc6ba55  refactor: extract inspection validation + summary rules from app-v2
343f8c4  release: 1.3-20260618  (= 현재 운영/origin)
```
PC 적용: `343f8c4`이면 `git am module-split-2commits.patch`(2커밋 통합).
이미 inspection만 적용했으면 `normalization-rules-split.patch`만. 적용 후 `npm run verify` → `git push origin main`.
둘 다 동작 동일 리팩토링이라 즉시 배포 불필요 — 다음 릴리스 때 토큰 bump하며 함께 내보낼 것
(새 모듈 `inspection-rules.min.js`/`normalization-rules.min.js`가 SW SHELL에 추가됨).

## What Changed (2026-06-18)

1. **운영 배포 1.3-20260618** (`343f8c4`, tag `v1.3-20260618`): 6/12 미배포분(월간 작업자 점검 집계 수정 +
   서약 과거 날짜 조회 + 통계·서약 기간 이력 범위 로딩)을 새 토큰/SW 버전으로 배포. alias 전환·서빙 확인.
   - 참고: 원격에 무관한 `v1.3-20260609`/`v1.4-20260609` 잔재 태그 있음(비단조). 핸드오프 규칙대로 1.3 채택.
2. **점검 검증/요약 분할** (`bc6ba55`): `assets/js/inspection-rules.js`(window.InspectionRules) 신설 —
   `validateInspectionDraft` + `summarizeInspectionChecks`. `submitInspection`이 위임. 5000케이스 차동 0 불일치.
3. **데이터 정규화 분할** (`1c8eddc`): `assets/js/normalization-rules.js`(window.NormalizationRules) 신설 —
   `normalizePendingSyncQueue`/`normalizeStatusRecords`/`normalizePendingPhotoUploads`. 부작용(uid/시계/사진변환/
   타임라인빌더)은 deps 주입. app-v2는 함수명·호출처 유지·본문만 위임. 4000케이스 차동 0 불일치.
   - 두 모듈 모두 11개 HTML + sw.js SHELL + verify 체인 등록, dist 재생성(커밋 포함). static-recovery 갱신.

## Known Issues / 환경 주의사항 (유효)

- **Cowork 마운트(virtiofs)에서 git lock·대용량 편집 절단 위험**: `.git/config.lock` 삭제 불가, 대용량 Write/Edit 무음 절단.
  → 이번 세션은 **네이티브 홈(~/) 클론에서 작업**해 회피. 큰 파일은 셸(sed/python)로 쓰고 `node --check` 재검증.
- `deploy-prod.bat`이 PC에서 실패(원인 미파악). 우회: 수동 `npm run build:assets` → `vercel --prod` → `vercel alias set <URL> gs-safety-checklist.vercel.app` 정상.
- 푸시/배포는 PC에서만. 샌드박스엔 GitHub 자격 증명 없음.

## Next Work (pick up here)

1. **데이터 정규화 부작용 함수 분할 (신중)** — `normalizeDataShape`(~60줄)·`dedupeChecklistItems`·`dedupeTools`·
   `dedupeShips`·`migrateOldChecklists`는 모두 `state`를 직접 변이. 순수화하려면 (state→새 state) 형태로 바꿔야 해
   결합이 깊음. 한 함수씩, 차동 검증 필수.
2. **작업지시서 타임라인 재설계** (06-12 스펙 확정, 코드 미착수) — 등록/점검시작/점검완료 3마일스톤, entry에
   `kind`/`actors`(worker id 배열) 추가 + 구버전 정규화. **구현 전 설계안 먼저 보여주고 확인받을 것.**
3. **이월**: `setupSignaturePad`는 캔버스/이벤트 결합이라 추출 가치 낮음(보류). `deploy-prod.bat` 디버깅.

## 분할 작업 패턴 (반드시 따를 것)

- 순수 로직: `*-rules.js` UMD 패턴(checklist-rules.js 참고). 부작용은 deps 주입(uid/now/외부함수). 전용 테스트 추가.
- app-v2는 함수명·호출처 보존, **본문만 모듈 위임** (호출처 다수일 때 안전).
- 등록 체크리스트: 11개 HTML `<script>`(dist .min.js + ?v=토큰, app-v2보다 앞) + `sw.js` SHELL +
  `package.json` verify 체인(--check + test) + `tools/build-assets.mjs`로 dist 재생성(커밋 대상).
- **static-recovery.test.js 주의**: app-v2 본문의 특정 표현식을 assert하는 라인이 있으면, 본문을 옮길 때
  깨진다 → 그 assert를 새 모듈 파일(`read("assets/js/xxx-rules.js")`)을 검증하도록 갱신.
- **검증**: 원본 로직을 oracle로 복제해 무작위 차동 검증(결정적 deps) → `npm run verify` → `node --check` → 커밋.

## How To Deploy ("배포 하자" 요청 시)

`VERSION.md` 규칙: 마이너 +0.1(→ `1.4-YYYYMMDD`), 새 asset token + SW 버전, VERSION.md 갱신, 커밋 + `v<버전>` 태그,
PC에서 main+태그 푸시, `vercel --prod` + `vercel alias set <URL> gs-safety-checklist.vercel.app`, 운영 URL 검증.
