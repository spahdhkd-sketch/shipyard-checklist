# GS 안전 체크리스트 웹페이지 분석 보고서

- 대상: 조선소 안전 체크리스트 (`gs-safety-checklist.vercel.app`)
- 버전: `1.2-20260610`
- 분석일: 2026-06-10
- 분석 범위: UX/디자인 · 성능/처리속도 · 코드 품질/구조 · 접근성

---

## 한눈에 보기

전체적으로 **잘 다듬어진 프로덕션 PWA**입니다. 보안 헤더(CSP), 오프라인 지원, 보안 경계 테스트, 캐시 버스팅 규율 등 운영 성숙도가 높습니다. 가장 큰 개선 여지는 **거대한 단일 파일 구조**(JS 약 1.3만 줄, CSS 약 1.2만 줄)와 **자체 자산의 빌드/최적화 부재**(번들·압축·이미지 포맷)입니다.

| 영역 | 평가 | 핵심 메모 |
|------|------|-----------|
| 보안/안정성 | 우수 | 엄격한 CSP, 보안 경계 단위테스트, RLS 기반 |
| 코드 품질/구조 | 보통 | 핵심 로직이 12,864줄 단일 파일에 집중 |
| 성능/처리속도 | 보통~양호 | SW 캐싱은 있으나 자체 JS/CSS 미압축, 이미지 미최적화 |
| UX/디자인 | 양호 | PC/모바일 모드, 명확한 한국어 카피, 오프라인 안내 |
| 접근성(a11y) | 보통 | aria/role·포커스 처리 있으나 다크모드·모션 축소 미흡 |

---

## 1. 장점 (Strengths)

### 보안 · 안정성
- **엄격한 Content-Security-Policy**를 모든 페이지에 적용. `default-src 'self'`, 스크립트는 해시 화이트리스트(`sha256-...`)로 제한, 인라인 스크립트 차단, `object-src 'none'`, `base-uri 'none'` 등 방어가 탄탄합니다.
- **보안 경계를 코드로 검증**합니다. `worker-security-static.test.js`, `admin-mutation-boundary-static.test.js`(약 24KB), `egress-optimization-static.test.js` 등 정적 보안/비용 회귀 테스트가 존재합니다.
- Supabase **anon 키만 클라이언트에 노출**(정상 패턴) — 실제 권한은 RLS로 통제하는 구조입니다.
- HTML 출력 시 `esc()` 이스케이프 헬퍼를 사용하고, `innerHTML` 사용처가 8곳으로 적고 대부분 내부 렌더 함수/이스케이프를 거칩니다 (XSS 표면 최소화).

### 코드 운영 성숙도
- **테스트 스위트가 충실**합니다(15개 파일). 규칙 로직(checklist/issue-material/pictogram/ship/worker), 대시보드 뷰, 작업준비 가시성, 정적 복구까지 커버. 핵심 단위테스트 6종은 실행 시 모두 통과 확인했습니다.
- **캐시 버스팅 규율**: CSS/JS에 `?v=20260610-...` 자산 토큰, 서비스워커 캐시 이름에 동일 토큰 반영 → 배포 시 갱신 누락 위험이 낮습니다.
- **버전·배포 규칙이 문서화**되어 있습니다(`VERSION.md`의 마이너 0.1 증가 규칙, 프로덕션 alias 검증 절차).
- `memory.md`에 "fast mode 범위 관리" 회고가 남아 있어 운영 학습이 축적되고 있습니다.

### PWA · 오프라인
- `manifest.json`, 서비스워커, 오프라인 폴백(`navigate` 실패 시 캐시된 `index.html`), `apple-mobile-web-app-*` 메타, 테마 컬러까지 갖춘 **설치형 모바일 앱 수준**입니다.
- **딥링크 정적 복구 패턴**: `check/items/manage/materials/ships/...` 각 라우트에 동일 셸 + `data-initial-view`를 가진 정적 HTML이 있어, JS 로딩 전·검색엔진·새로고침 상황에서도 진입점이 안정적입니다.
- 푸시 알림(VAPID) 및 스타일 템플릿(안내/주의 등) 체계가 구축돼 있습니다.

### UX
- **PC/모바일 모드 토글**과 모바일 하단 내비, 상단 앱바 등 현장(모바일 우선) 사용을 고려한 레이아웃.
- "임시저장됨 · 연결 시 전송" 같은 **오프라인/동기화 상태 안내**가 명시적입니다.
- 작업 전 시작 게이트("작업당일 07:00부터 점검 시작 가능합니다")처럼 **현장 업무 규칙이 UI에 반영**돼 오용을 줄입니다.
- 조선소 작업별 일러스트 54종으로 항목 인지를 돕습니다.

---

## 2. 단점 (Weaknesses)

### 코드 구조
- **`app-v2.js`가 12,864줄 단일 파일**(함수/화살표 약 1,519개)이며 섹션 구분 주석조차 없습니다. 신규 인력 온보딩, 변경 영향 추적, 충돌 위험이 큽니다.
- **`styles-v2.css`가 11,550줄 단일 파일**. 컴포넌트별 분리(20-/30- 접두)가 일부 시작됐지만 본체 대부분이 한 파일에 남아 있습니다.
- CSP가 **모든 HTML `<head>`에 동일하게 중복**되어, 정책 한 줄 변경에 10여 개 파일 동시 수정이 필요합니다.
- Supabase URL이 여러 정적 파일과 JS에 반복 하드코딩되어 환경 분리(스테이징/프로덕션)가 어렵습니다.

### 성능/처리속도
- **자체 JS/CSS에 빌드·압축·번들 단계가 없습니다.** 벤더(supabase-js)만 minified(196KB)이고, 핵심 1.3만 줄 JS·1.2만 줄 CSS는 원본 그대로 전송됩니다(트리셰이킹·압축 미적용).
- **서비스워커의 CSS/JS 전략이 network-first**입니다. 온라인 사용자는 매 방문 시 네트워크 왕복을 먼저 기다린 뒤에야 화면이 갱신됩니다(신선도 우선 → 체감 속도 손해).
- **일러스트 1.3MB / PNG 54장**, WebP/AVIF 미적용, `width/height` 속성·반응형 이미지·지연로딩(폴백 외) 부재로 초기 페인트·레이아웃 시프트에 불리합니다.
- 중복 추정 자산(`confined.png` vs `confinedSpace.png` 등)으로 전송·관리 비용이 증가할 수 있습니다.

### 접근성(a11y)
- `prefers-color-scheme` 미사용 → **다크모드 없음**(야외·야간 현장에서 눈부심/배터리 이슈).
- `prefers-reduced-motion` 처리가 사실상 1곳뿐이라 모션 민감 사용자 대응이 부족합니다.
- 터치 타깃 최소 크기(44~48px) 적용 범위가 일부에 그쳐 보여, 장갑 착용 현장 사용성 점검이 필요합니다.

---

## 3. 개선 제안 (Recommendations)

우선순위 순(효과/노력 기준).

### 즉시 효과가 큰 항목
1. **이미지 최적화**: 일러스트를 WebP/AVIF로 변환하고 `<img width height loading="lazy">`를 부여. 1.3MB 자산을 대략 절반 이하로 줄이고 레이아웃 시프트를 제거할 수 있습니다.
2. **서비스워커 CSS/JS 전략을 stale-while-revalidate로 변경**: 캐시본을 즉시 그려 체감 로딩을 0에 가깝게 하고, 백그라운드로 갱신. 자산 토큰이 이미 있어 안전하게 적용 가능합니다.
3. **빌드 단계 도입(esbuild 등)**: 자체 JS/CSS를 번들·압축. 전송량 감소 + 파싱 시간 단축. 기존 `verify` 스크립트와 자연스럽게 연결됩니다.

### 구조/유지보수
4. **`app-v2.js` 모듈 분할**: 이미 `dashboard-view/screen-views/*-rules/*-helpers`로 일부 분리돼 있으니, 같은 패턴으로 도메인별(인증/관리/푸시/렌더)로 계속 쪼개 단일 파일을 축소.
5. **CSP를 HTTP 헤더로 일원화**: 각 HTML 인라인 대신 `vercel.json`의 `headers`로 통일 → 중복 제거, 정책 변경 1곳화.
6. **환경 설정 외부화**: Supabase URL/키를 빌드 주입 또는 단일 config로 분리해 스테이징/프로덕션 분리.

### 접근성/UX
7. **다크모드 추가**(`prefers-color-scheme`)와 **모션 축소 범위 확대**.
8. **a11y 감사 1회 실시**: 포커스 표시(`:focus-visible`) 전수 적용, 터치 타깃 44px+ 보장, 색 대비 점검.

---

## 부록: 근거 데이터

| 항목 | 측정값 |
|------|--------|
| `app-v2.js` 라인 수 | 12,864 |
| `styles-v2.css` 라인 수 | 11,550 |
| 자체 JS 합계 | 14,357줄 (9개 파일) |
| CSS 합계 | 12,964줄 / 288KB |
| 일러스트 | PNG 54장 / 1.3MB |
| 벤더 supabase-js | 196KB (minified) |
| 배포 용량(.git 제외) | 약 12MB |
| 테스트 파일 | 15개 (보안 경계·egress 포함) |
| `innerHTML` 사용처 | 8곳 (esc/내부 렌더 경유) |
| JS 내 aria/role | 112회 |
| SW 캐시 전략 | navigate=network→캐시폴백, CSS/JS=network-first, 기타=cache-first |

> 참고: 본 분석은 소스 코드 정적 검토와 핵심 단위테스트 실행 결과에 기반합니다. 실제 로딩 시간·Lighthouse 점수 등 런타임 지표는 별도 측정을 권장합니다.
